<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-black text-gray-100">
    
    <div class="min-h-screen flex flex-col items-center justify-center p-6">   
        <div class="text-3xl font-bold mb-8 text-blue-500 shadow-md shadow-blue-500">
            Task Manager
        </div>
        
        
        
        
        
        
        <div class="form bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-lg">
            <form action="/create" method="post">
                <input class="block w-full px-4 py-3 mb-4 bg-gray-700 text-white rounded-md placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500" type="text" placeholder="Title goes here" name="title">
                <textarea class="block w-full px-4 py-3 mb-4 bg-gray-700 text-white rounded-md placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Write your text" name="detail"></textarea>
                <input class="block w-full px-4 py-3 bg-blue-600 text-white rounded-md cursor-pointer hover:bg-blue-700 transition" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks mt-8 w-full max-w-lg">
            <% if (files.length > 0) { %>
                <% files.forEach((val) => { %>
                    <div class="task px-6 py-4 bg-gray-800 mb-4 rounded-lg shadow-md">
                        <h1 class="text-white text-2xl font-semibold mb-2">
                            <%= val %>
                        </h1>
                        <div class="flex flex-col space-y-2">
                            <a href="/file/<%= val %>" class="text-blue-400 hover:underline">Read more</a>
                            <a href="/edit/<%= val %>" class="text-blue-400 hover:underline">Edit</a>
                            <a href="/remove/<%= val %>" class="text-blue-400 hover:underline">Remove</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="text-white text-center text-2xl bg-gradient-to-r from-red-400 to-yellow-500 bg-clip-text text-transparent">
                    No tasks available
                </p>
                
                
                
                
            <% } %>
        </div>
    </div>
</body>

</html>
